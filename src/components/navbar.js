const Navbar = () => {
  return (
    <div
      className="flex flex-col justify-center items-center w-[100vw] mt-[50px] text-[50px]"
      style={{ fontFamily: "Koulen" }}
    >
      Learning Matters
    </div>
  );
};

export default Navbar;
